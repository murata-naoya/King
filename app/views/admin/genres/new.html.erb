<% if @genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= @genre.errors.count %> error prohibited this user from being saved: </h2>
      <ul>
      <% @genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
     <% end %>
      </ul>
    </div>
<% end %>

<div id="genre">
<h2>ジャンル登録画面</h2>
<%= form_for([:admin, @genre]) do |f| %>
<h4>ジャンル名</h4>
<%= f.text_field :name, class: "form-control", placeholder: "ジャンル名を入力" %><br>
<%= f.submit "投稿", class: "form-create" %>
<% end %>
</div>


